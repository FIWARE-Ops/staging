var types = [
{{#each types}}
    {{#if (ne this '')}}
        '{{this}}',
    {{/if}}
{{/each}}
]; var domains = [
{{#each domains}}
    {{#if (ne this '')}}
        '{{this}}',
    {{/if}}
{{/each}}
]; var technologies = [
{{#each technologies}}
    {{#if (ne this '')}}
        '{{this}}',
    {{/if}}
{{/each}}
];

 var modalData = [
{{#each enablers}}
    { 
        name: "{{name}}", 
        img: "{{img}}",
        type: "{{type}}", 
        badge: "{{{badge}}}", 
        content: "{{{content}}}",  
        domain: "{{domain}}",  
        technology: "{{technology}}",
        status: "{{status}}",
        gitHub: "{{gitHub}}",
        docker: "{{docker}}",
        documentation: "{{documentation}}",
    },
{{/each}}
];