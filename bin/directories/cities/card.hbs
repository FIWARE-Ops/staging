{{#each this}}
    <div class="grid-item  {{createClass type}} {{createClasses domain}} {{createClass country}}">
        <a href="./city-details?id={{createClass city}}">
            <img class="gridElementLogo" src="{{thumb}}"  
            {{#if (gt @index 9)}}loading="lazy"{{/if}}>
        </a>
        <div class="gridElementTextContainer">
            <a href="./city-details?id={{createClass city}}">
            <div class="label-location">
                <h3>{{country}}</h3>
                <img class="flag" alt="{{country}}"src="{{flagUrl}}" 
                 {{#if (gt @index 9)}}loading="lazy"{{/if}}>
            </div>
            <h6 class="ihub-name">{{city}}</h6>
            </a>
            <div class="icons">
                <a class="cat-website" target="_blank" href="./city-details?id={{createClass city}}" rel="noopener">
                    <span class="icon website"></span>
                </a>
            </div>
        </div>
    </div>
{{/each}}