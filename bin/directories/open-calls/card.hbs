{{#each this}}
    <div class='grid-item {{createClass type}} {{createClasses domain}}'>
        <a target="_blank" href="{{website}}" rel="noopener">
            <span class="cat-name hidden">{{createClass name}}</span>
            <img decoding="async" class="gridElementLogo" src="{{img}}">
        </a>
        <div class="gridElementTextContainer">
            <a target="_blank" href="{{website}}" rel="noopener">
                <h4 class="excerpt-rd">{{content}}</h4>
                <h4 class="oc-status">{{type}}</h4>
            </a>
            {{#if (gt domain.length 0)}}
                <div class='chip-domain'>
                    <ul class='chips'>
                    {{#each domain}}
                        <li>{{this}}</li>
                    {{/each}}
                    </ul>
                    <span class='chips-gradient'></span>
                </div>
            {{/if}}

            <div class="oc-attributes">
               <div class="oc-attribute">
                 <span class="material-symbols-outlined">payments</span>
                 <h3 class="grant">{{grant}}</</h3>
               </div>
               <div class="oc-attribute">
                 <span class="material-symbols-outlined">event</span>
                 <h3 class="closeDate {{#if (eq type 'Closed')}}closed{{/if}}"> {{formatDate closeDate}}</h3>
               </div>
            </div>

            <div class='icons'>
                {{#if (eq type "Closed")}}
                <a class='cat-details-primary closed' target="_blank" href="{{website}}" rel="noopener">
                    <span class="material-symbols-outlined">info</span>
                    Closed
                </a>
                {{/if}}
                {{#if (eq type "Open")}}
                 <a class='cat-details-primary' target="_blank" href="{{website}}" rel="noopener" data-close-date="{{parseDate closeDate}}">
                     <span class="material-symbols-outlined">open_in_new</span>
                    Apply
                </a>
                {{/if}}
            </div>
        </div>
    </div>
{{/each}}