{{#each this}}


<div class='grid-item {{createClass track}} {{createClass session}}{{#each speakers}} {{createClass name}}{{/each}} {{createClass shortDate}}'>
    <div class="gridElementTextContainer">
      <div class="chip-track">
        <div class="track {{createTrack track}}">{{track}}</div>
      </div>
      <p class="panel-title">{{title}}</p> 
      <span class="excerpt">{{session}}</span>
      <div class="panel-info">
        <div class="date-time">
          <h3 class="date">{{shortDate}}</h3>
          <span class="separator-dot"></span>
          <span class="time">{{start}} – {{end}}</span>
        </div>
        <span class="separator-dot"></span>
        <span class="place">{{location}}</span>
      </div>
    </div>
    <div class="speakers-container">
      <div class="speakers">
        {{#each speakers}}
        <div class="speaker">
          <div class="profile-picture">
            <img decoding="async" alt="" src="{{img}}" loading="lazy">
          </div>
          <div class="speaker-info">
            <div class="speaker-name">{{name}}</div>
            <div class="speaker-job-title">{{shortJob}}</div>
            <div class="speaker-company">{{company}}</div>
          </div>
        </div>
        {{/each}}
      </div>
        <span class="speakers-gradient"></span>
    </div>
  </div>
{{/each}}
